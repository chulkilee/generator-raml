{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "gulp build",
    "clean": "gulp clean",
    "serve": "gulp serve",
    "watch": "gulp watch",
    "webserver": "gulp webserver"
  },
  "dependencies": {
    "gulp": "^3.8.8",
    "gulp-load-plugins": "^0.6.0",
    "gulp-open": "^0.2.8",
    "gulp-plumber": "^0.6.5",
    "gulp-raml2html": "^0.1.7",
    "gulp-rimraf": "^0.1.0",
    "gulp-util": "^3.0.1",
    "gulp-webserver": "^0.8.1"
  }
}
